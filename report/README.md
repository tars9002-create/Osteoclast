# Report Runtime Outputs

This directory contains small support code and runtime output locations used by the figure
scripts.

- `pub_style.py` defines shared plotting colors and figure-export helpers.
- `tables/` is reserved for generated tabular summaries used by figure rendering.

Generated tables and report figures are ignored by Git except for `.gitkeep` placeholders.
